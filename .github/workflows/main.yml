name: first workflow

on:
    push:
        branches:
            - main

jobs:
    test:
        runs-on: ubuntu-latest
        
        steps:
            - name: checkout
              uses: actions/checkout@v7.0.0

            - name: message for a hello world! 
              run: |
                echo "hello world"
                run : ls -l 
            